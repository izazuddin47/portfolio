1) Add `contact_messages` table to `database/schema.sql`.
2) Re-run schema in phpMyAdmin (or run only the new CREATE TABLE query).
3) Submit contact form and verify row exists in `portfolio_db.contact_messages`.
4) If CSRF prevents insert, verify session/csrf generation in `index.php`.

